import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // No special configuration needed!
  // The gif2vid/browser export provides a browser ESM bundle with embedded WASM

  experimental: {},
};

export default nextConfig;
